{
  "name": "wp-migration-control-server",
  "version": "1.0.0",
  "private": true,
  "description": "Optional local API for wp-migration-poc (spawn pipeline scripts)",
  "scripts": {
    "dev": "node index.js",
    "dev:all": "concurrently -n api,web -c blue,green \"node index.js\" \"npm run dev --prefix ../web\"",
    "start": "cross-env NODE_ENV=production node index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
